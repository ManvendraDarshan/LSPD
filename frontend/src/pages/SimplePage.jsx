export default function SimplePage({ title }) {
  return <div className="page-shell"><section className="card p-6"><h1 className="text-3xl font-black">{title}</h1><p className="mt-3 text-slate-600">This page is part of the LSPD workflow shell and is ready to connect to deeper feature screens as the product grows.</p></section></div>;
}
